# 진행상황 (새 대화창에서 이어갈 때 이 파일부터 읽어줘)

## 프로젝트
초6 딸(김로아)을 위한 여름방학 학습 자동화. 매일 카카오톡으로 링크 발송 → 사회/과학/수학/영어 개념+문제 10개씩 풀기 → 자동 채점 후 본인+부모(아빠/엄마)에게 카톡으로 결과 발송.

## 완료된 것
- 카카오 로그인 + "나에게 보내기"로 학생/부모 각자 카톡 수신 (`lib/kakao.js`, `pages/login.js`, `pages/api/auth/kakao/*`)
- Vercel 배포 + 매일 자동발송 크론 (`vercel.json`, `pages/api/cron/daily.js`, 매일 KST 오전 10시)
- Upstash Redis로 토큰/진행상황/결과/오답노트 영구 저장 (`lib/store.js`)
- 오답노트, 이미지(SVG) 지원, 난이도 자동조정(하/중/상)
- 학생은 `daughter`(김로아, 초6) 1명만 활성 (`lib/users.js`). 부모는 아빠/엄마 2명.
- 1학기 범위로만 출제하도록 제한 (`lib/units.js`의 `SEMESTER`, 프롬프트에 명시)
- **문제은행 대량 사전생성 시스템** (신규, 이번 세션에 추가):
  - `scripts/generate-bank.mjs` — 초6 전과목 × 중/상 난이도(하는 일부러 안 만듦, 아래 참고) × 단원별로 AI에게 문제 20개씩 + 단원당 심화 개념 설명 1개를 미리 만들어 `lib/curriculum/g6-bank.json`에 저장하는 1회성 스크립트. **사용자(로아빠) 컴퓨터에서 직접 실행 중** (내 샌드박스는 api.anthropic.com 접근이 막혀 있어 직접 못 돌림).
  - `lib/curriculum/bank.js` — 그 결과 파일을 읽어오는 로더.
  - `lib/daily.js` — 우선순위를 캐시 → **문제은행(g6-bank.json)** → (은행에 없으면, 예: "하" 난이도) 실시간 AI 생성 → 정적 폴백 순으로 변경. 이제 매일 크레딧이 거의 안 듦.
  - `pages/index.js` — "📦 미리 준비된 문제" 배지 추가.
  - 왜 이렇게 바꿨나: 매일 AI 실시간 호출 시 크레딧 소모가 컸음. "하" 난이도는 오답이 많아질 때만 필요하므로 미리 만들지 않기로 함(로아빠 판단).

## 🚨 중요 정책 변경 (2026-07-31, 로아빠 지시 — 반드시 지킬 것)
**로아빠 허락 없이 Anthropic API 크레딧을 절대 쓰지 말 것.** (본인 표현: "난 가난한 직장인이야")
- `lib/generate.js`에 `liveFallbackEnabled()` 추가함 — `ALLOW_LIVE_AI_FALLBACK` 환경변수가 정확히 `"true"`일 때만 실시간 AI 생성이 동작. 기본값(변수 없음)은 항상 꺼짐.
- `lib/daily.js`의 실시간 AI 생성 분기도 `liveFallbackEnabled() && hasApiKey()`로 막아둠.
- `scripts/generate-bank.mjs`(로컬에서 API로 대량생성하던 스크립트)도 이제 쓰지 않기로 함 — 대신 **Claude Pro 채팅에서 내(Claude)가 직접 문제를 써서 파일에 넣는 방식**으로 전환(API 크레딧 0원). 앞으로 이 스크립트를 실행하거나 `ALLOW_LIVE_AI_FALLBACK`을 켜려면 반드시 로아빠에게 먼저 물어볼 것.
- **문제은행("중"/"상")을 20개 → 60개로 확장 중.** "하" 난이도는 전 과목(수학 포함) 절대 새로 만들지 않음(로아빠 지시).
- 이 확장 작업을 위해 예약 작업(`daughter-quiz-bank-expand`, 매일 새벽 4시)을 등록해둠 — 매일 자동으로 이어서 채움. 작업 파일: `C:\Users\uknow\Claude\Scheduled\daughter-quiz-bank-expand\SKILL.md`.

## 지금 상태 (2026-07-31 새벽 예약 작업 실행 후)
`lib/curriculum/g6-bank.json` 단원/난이도별 문항 수 (총 46개 조합 = 23단원 × 중/상, "하"는 별도, 완료 기준 60개):
- 수학 6단원: 하 20(더 안 늘림) / **분수의 나눗셈, 각기둥과 각뿔 중/상 각 60 완료** / 나머지 4단원(소수의 나눗셈, 비와 비율, 여러 가지 그래프, 직육면체의 부피와 겉넓이) 중/상 각 20~21 — 확장 대기
- 과학 4단원: 중 20 / 상 20 — 확장 대기
- 사회 6단원: 중 20 / 상 20 — 확장 대기
- 영어 7단원: **일반동사 3인칭 단수만 중/상 각 60개 완료**, 나머지 6단원은 중/상 각 20 — 확장 대기

전체 진행률: 46개 조합 중 6개 완료 (수학 2단원 + 영어 1단원 × 중/상).

git 로그(로컬 커밋 완료, **push는 사용자가 직접**):
```
(이번 세션 커밋이 맨 위에 추가됨 - 아래는 이전 로그)
6b4cdf4 영어 3인칭단수 단원 60문항 확장 + 기존 수학 중복선택지 버그 수정
5d0fc84 영어 문제은행 완성(280문항) + 실시간 AI 폴백 기본 비활성화
d6842f8 사회 문제은행 완료분 반영
```

## ⚠️ 다음에 이어갈 것
1. **git push 확인** — 위 3개 커밋이 GitHub에 반영됐는지 확인(`git log origin/main..HEAD`로 안 올라간 커밋 확인). 안 됐으면:
   ```
   cd daughter-quiz-app
   git push
   ```
2. **문제은행 확장 계속** — 예약 작업이 매일 새벽 4시에 자동으로 46개 조합을 60개씩 채워나감. 진행 상황은 `lib/curriculum/g6-bank.json`에서 직접 세어보거나, 예약 작업 실행 완료 알림으로 확인.
3. 배포 후 실제 폰에서 링크 열어서 문제/개념이 잘 나오는지 확인.
4. 이후 데스크톱 정리: `lib/curriculum/g6_more.js` 등 기존 폴백 문제은행에 남아있는 2학기 내용 정리(선택사항, 급하진 않음).

## 참고
- PDF 자료(`data/` 폴더): EBS Math 무료 자료(수학 6개)는 참고 가능, `초등오투과학6-1.pdf`(비상교육 시중교재)는 저작권상 그대로 쓰면 안 됨 → 사용 안 함.
- Anthropic API 키는 `.env.local`에 안 넣음(로아빠가 직접 관리). Vercel의 `ANTHROPIC_API_KEY`는 "Sensitive" 타입이라 콘솔에서 재조회 불가 — 필요하면 새로 발급해야 함.
